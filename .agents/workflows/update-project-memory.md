# Workflow: Update Project Memory
<!-- DYNAMIC FILE -->

1. Identify the lesson: mistake, decision, rule, or workflow improvement.
2. Update shared `docs/ai/` files first.
3. Synchronize `gemini/GEMINI.md`, `AGENTS.md`, `.agents/rules/*.md`.
4. Synchronize other tool adapters per `docs/ai/tool-sync-policy.md`.
5. Propose commit: `ai(memory): ...`
