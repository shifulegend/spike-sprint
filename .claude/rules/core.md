# Core Rules — All Code
<!-- DYNAMIC FILE -->

- Write the smallest sensible module or function per responsibility.
- No hard-coded values. Use config, env vars, schemas, or feature flags.
- State assumptions explicitly before implementing.
- Inspect existing patterns before introducing new abstractions.
- Verify: lint → typecheck → test → build.
- Keep scope tight; avoid speculative refactors unless explicitly asked.
